import React from 'react';
require("dotenv").config();

function Api() {

  return (
    <div>
    <h1>Below should be Pusher APP ID</h1>
    
      
    </div>
  );
}

export default Api;

// "start": "cd client && npm start"

// "start": "react-scripts start"
